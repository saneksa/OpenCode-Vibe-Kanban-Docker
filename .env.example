# Timezone configuration
TZ=Europe/Moscow

# Security Configuration
# ----------------------
# Password for OpenCode Web UI (required)
# If not set, a random password will be generated on startup.
OPENCODE_SERVER_PASSWORD=

# SSH Public Key for secure access (recommended)
# Paste your public key here (e.g., contents of ~/.ssh/id_rsa.pub)
# If set, password authentication for SSH will be disabled.
SSH_PUBLIC_KEY=
